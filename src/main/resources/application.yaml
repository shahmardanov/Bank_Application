spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://localhost/bankapp
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      username: root
      password: 123
  liquibase:
    enabled: false